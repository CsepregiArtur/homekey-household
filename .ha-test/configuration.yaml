# Disposable Home Assistant configuration for HomeKey Household validation.
# Generated by tools/integration/ha_harness.py — do not edit by hand.
default_config:

logger:
  default: warning
  logs:
    custom_components.homekey_household: debug
    homeassistant.components.mqtt: info

mqtt:
  broker: 127.0.0.1
  port: 18830
  discovery: false

# HomeKey Household is added through the config flow (UI) or via a config entry
# created programmatically by the validation harness.
