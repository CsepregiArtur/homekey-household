{
  "config": {
    "step": {
      "user": {
        "title": "Set up MQTT",
        "description": "HomeKey Household uses the official Home Assistant MQTT integration as its transport. MQTT is not configured yet — choose how you want to continue.",
        "menu_options": {
          "mqtt_guide": "Walk me through the MQTT setup",
          "mqtt_express": "Configure MQTT for me (express)",
          "mqtt_addon": "Use the MQTT add-on (Home Assistant OS only)"
        }
      },
      "mqtt_guide": {
        "title": "Configure MQTT",
        "description": "Install and configure the official MQTT integration, then come back and press Submit.\n\n1. Go to [Settings → Devices & Services](/config/integrations).\n2. Click **Add Integration** and choose **MQTT**.\n3. Enter your broker host/port (and credentials if required) and finish the wizard.\n4. Press **Submit** below to re-check the connection.\n\nThe integration is a pure consumer of the firmware MQTT topics; the broker can be Mosquitto or any MQTT broker.\n\nDocumentation: {docs}"
      },
      "mqtt_express": {
        "title": "Configure MQTT automatically",
        "description": "Enter your broker details. HomeKey Household will create the official MQTT integration entry for you.\n\nUse the MQTT protocol version 5 (the default; the firmware is also compatible with 3.1.1). Host 127.0.0.1 is only correct if the broker runs on the same host as Home Assistant."
      },
      "mqtt_addon": {
        "title": "Use the MQTT add-on",
        "description": "Install and start the official Mosquitto broker add-on via Supervisor, then wire up the MQTT integration automatically.\n\nThis needs no broker details. It is available only on Home Assistant OS or a Supervised installation, because only there can Supervisor install the add-on. Press **Submit** to continue."
      },
      "household": {
        "title": "HomeKey Household",
        "description": "Configure a HomeKey household. MQTT is reused from the Home Assistant MQTT integration. The optional recovery secret is used once to derive the household command key locally and is never stored or transmitted.",
        "data": {
          "household_id": "Household ID",
          "household_name": "Household name",
          "recovery_secret": "Recovery secret (optional)",
          "salt": "KDF salt (optional)",
          "command_control": "Enable authenticated lock control"
        },
        "data_description": {
          "household_id": "Matches the household_id published on the household MQTT topics, e.g. HOME-001.",
          "recovery_secret": "Used only to derive the HMAC command key. Never sent over MQTT and never stored.",
          "salt": "The salt used by the firmware when deriving the command key, if one is configured.",
          "command_control": "When disabled the lock entity is read-only. Commands fail closed without a credential."
        }
      }
    },
    "error": {
      "invalid_household_id": "Household ID must be 1-64 characters of [A-Za-z0-9_-].",
      "invalid_recovery_secret": "The recovery secret must not be empty.",
      "mqtt_not_configured": "MQTT is still not configured or not connected. Finish the MQTT setup, then try again.",
      "mqtt_setup_failed": "Could not create the MQTT integration automatically. Please add the MQTT integration manually from Settings → Devices & Services, then try again.",
      "cannot_connect": "Home Assistant could not reach the MQTT broker at that address. Check the host and port, and make sure the broker is running and reachable from this machine. Note: 127.0.0.1 only works if the broker runs on the same host as Home Assistant.",
      "invalid_auth": "The broker rejected the username/password.",
      "bad_ws_headers": "The WebSocket headers are invalid.",
      "mqtt_addon_failed": "Could not install or start the Mosquitto broker add-on. Check Supervisor, then try again or use the express setup instead."
    },
    "abort": {
      "already_configured": "This household is already configured.",
      "not_hassio": "The MQTT add-on option is only available on Home Assistant OS or a Supervised installation."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HomeKey Household options",
        "description": "Rotate the command credential or change availability settings. Leaving the recovery secret blank keeps the current key.",
        "data": {
          "recovery_secret": "Recovery secret (leave blank to keep current key)",
          "salt": "KDF salt (optional)",
          "command_control": "Enable authenticated lock control",
          "legacy_client_id_prefix": "Legacy MQTT client-id prefix (shared LWT)"
        },
        "data_description": {
          "legacy_client_id_prefix": "Prefix of the legacy client id (default ESP_). Used only to read the shared broker LWT availability topic."
        }
      }
    },
    "error": {
      "invalid_recovery_secret": "The recovery secret must not be empty."
    }
  },
  "entity": {
    "lock": {
      "lock": {
        "name": "Lock"
      }
    },
    "binary_sensor": {
      "online": {
        "name": "Node online"
      }
    },
    "sensor": {
      "health": {
        "name": "Node health"
      },
      "backup": {
        "name": "Backup status"
      },
      "security": {
        "name": "Security status"
      },
      "firmware": {
        "name": "Firmware version"
      },
      "last_auth": {
        "name": "Last HomeKey authentication"
      }
    }
  },
  "entity_component": {
    "lock": {
      "name": "Lock"
    }
  }
}
